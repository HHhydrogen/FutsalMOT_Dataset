[project]
name = "futsalmot-code"
version = "0.1.0"
description = "FutsalMOT Unreal Engine synthetic dataset generation scripts"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py310"
