# Task 1 报告

## 实现

- 增加 Config v3 严格模型 `TaskConfigV3` 及仿真、输出嵌套模型。
- 增加 `LocalMachineConfig`，仅接受本机路径和环境字段。
- 增加 `TASK_V3_SCHEMA` 与 `LOCAL_CONFIG_ENV` 常量。
- loader 支持按 schema 加载 v3，同时保持 v2 加载路径不变。
- 增加本机配置示例，并忽略 `configs/local.machine.json`。
- 更新配置测试和仓库配置卫生测试。

## TDD 记录

先添加 v3 和本机配置测试，再运行：

```text
uv run pytest tests/test_task_config.py -q
```

结果：失败（收集阶段），原因是实现前不存在 `LocalMachineConfig`，错误为：

```text
ImportError: cannot import name 'LocalMachineConfig' from 'grf_ue_bridge.config.models'
!!!!!!!!!!!!!!!!!!!! Interrupted: 1 error during collection !!!!!!!!!!!!!!!!!!!!
```

实现后首次运行目标测试为 `42 passed, 1 failed`；唯一失败是 Windows 默认 `gbk` 解码 `.gitignore`。测试改为显式 UTF-8 后重新验证通过。

## 验证

```text
uv run pytest tests/test_task_config.py -q
...........................................                              [100%]
43 passed in 0.16s
```

```text
uv run pytest -q
........................................................................ [ 11%]
........................................................................ [ 22%]
........................................................................ [ 33%]
........................................................................ [ 44%]
........................................................................ [ 55%]
........................................................................ [ 66%]
........................................................................ [ 77%]
........................................................................ [ 88%]
........................................................................ [100%]
648 passed, 7 deselected in 12.01s
```

## 提交

提交信息：`增加Config v3和本机配置模型`

## 关注事项

- v3 loader 已支持 schema 分派，但 resolver/CLI 尚未扩展，这是后续任务范围。
- `configs/local.machine.example.json` 是占位符示例，仓库卫生测试已明确跳过该机器配置模板。
